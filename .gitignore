test/node_modules

# Variables de entorno con credenciales — NUNCA al repositorio
.env
.env.prod
.env.local
.env.dev

# Credenciales de PgDog — contiene passwords reales, NO al repositorio
# Usar users.toml.example como plantilla y generar el real en el servidor
users.toml

# Logs de Docker / aplicación
*.log